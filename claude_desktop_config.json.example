# Example MCP Server Configuration for Claude Desktop
#
# This is an example configuration file for Claude Desktop on Windows.
# Copy this content to: %APPDATA%\Claude\claude_desktop_config.json
#
# Steps:
# 1. Press Windows+R and type: %APPDATA%
# 2. Navigate to Claude folder
# 3. Create or edit: claude_desktop_config.json
# 4. Paste this configuration
# 5. Save the file
# 6. Right-click Claude icon in system tray → Quit (fully exit)
# 7. Restart Claude Desktop

{
  "mcpServers": {
    "excel-live-injector": {
      "command": "python",
      "args": [
        "-m",
        "excellm"
      ],
      "description": "Real-time Excel automation for open files using Windows COM",
      "env": {
        "PYTHONUNBUFFERED": "1",
        "LOG_LEVEL": "INFO"
      }
    }
  }
}
